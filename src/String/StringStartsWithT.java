package String;

import java.util.Scanner;

public class StringStartsWithT {

	public static void main(String[] args) {
		Scanner sc=new Scanner(System.in);
		 // Array of strings
        String[] strings = {"Apple", "Banana", "Tomato", "Tiger", "Orange", "Table"};
        
//        System.out.println("Strings that start with 'T':");
        System.out.println("Enter the String:");
        String s= sc.next();
        
        // Iterate through the array and check the first character
        for (String str : strings) {
//            if (str.charAt(0) == 'T') { // Check if first char is 'T'
//                System.out.println(str);
//            }
//        	if(str.startsWith("T")&& str.endsWith("o")) {
//        		System.out.println(str);
//        	}
        	
        }

	}

}
